apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certificadora-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "certificadora-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "certificadora-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
